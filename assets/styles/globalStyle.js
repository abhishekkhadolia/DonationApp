const {StyleSheet} = require('react-native');
const {backgroudWhite} = require('../helper');

const globalStyle = StyleSheet.create({
  backgroundWhite: backgroudWhite,
});

export default globalStyle;
